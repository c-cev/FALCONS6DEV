integer a =10;
integer b = 36;
integer big = math.max(a, b);
system.debug(big);

//write a number between 1 to 10
decimal randomnum = math.floor(math.random()*10+1);
system.debug(randomnum);

TurkishLesson5.listMethod();